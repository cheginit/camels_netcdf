name: camels
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.9

  - dask-core
  - geopandas>=0.8
  - h5netcdf
  - netcdf4
  - pandas>=1.0
  - pyarrow
  - pygeos
  - pyproj>=1.2
  - rich
  - rioxarray
  - shapely
  - xarray>=0.17

  # optional deps to speed up xarray
  - bottleneck

  # plotting and dev deps
  - proplot
  - ipykernel
  - jupyterlab_widgets
  - ipywidgets
